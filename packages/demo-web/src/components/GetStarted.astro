---
import Bubbles from './Bubbles.astro';
---

<section id='get-started'>
  <Bubbles />
  <picture>
    <img
      src='/images/z.webp'
      alt='z-ui logo'
      class='z-logo'
    />
    <img
      src='/images/z.webp'
      alt='z-ui logo background'
      class='img-background'
    />
  </picture>
  <p>
    Zephyra UI is a modern and accessible reusable web component library
    designed to make it easy to develop user interfaces with high-performance
    components. Built with Lit, and TypeScript, Zephyra UI offers a set of
    ready-to-use components that can be easily integrated into any web
    application. Its modular and lightweight design allows developers to
    customize and extend each component to their needs.
  </p>

  <a
    class='get-started'
    href='/usage'
    >Get Started</a
  >
</section>

<style>
  #get-started {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    max-height: 100dvh;
    overflow: hidden;
  }

  section {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    height: 100%;
    width: 100%;

    position: relative;
  }

  p {
    text-align: center;
    font-size: larger;
    text-wrap: balance;
    max-width: 60%;
    margin-top: 40px;
    padding: 10px;
    backdrop-filter: blur(12px);
    border-radius: 8px;
    background: rgba(0, 0, 0, 0.05);
    color: var(--text-s-color);
  }

  img {
    aspect-ratio: 1/1;
    width: 200px;
    border-radius: 5px;
  }

  picture {
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .z-logo {
    position: relative;
    z-index: 3;
    width: 150px;
  }

  .img-background {
    position: absolute;
    z-index: 1;

    filter: blur(35px);
  }

  .get-started {
    z-index: 3;
    border-radius: 5px;
    color: var(--text-color);
    text-decoration: none;
    font-weight: bold;
    background-color: rgba(var(--call-to-action), 0.25);
    padding: 8px 14px;
    backdrop-filter: blur(12px);
  }
</style>
