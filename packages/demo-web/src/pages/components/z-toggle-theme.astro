---
import ContentLayout from '../../layouts/ContentLayout.astro';
import MainLayout from '../../layouts/MainLayout.astro';
import { Code } from 'astro-expressive-code/components';
---

<MainLayout>
  <ContentLayout>
    <h2>z-toggle-theme</h2>

    <div class='info'>
      <article class='info-content'>
        <h3>Properties:</h3>
        <section class='prop'>
          <details open>
            <summary>isLightTheme</summary>
            <p>
              The <code>isLightTheme</code> is a internal state who determines the
              current theme of the component, Default is system. When set to <code
                >true</code
              >, the light theme is active, and the component displays a
              sun-like appearance. When set to <code>false</code>, the dark
              theme is active, and the component displays a moon-like
              appearance.
            </p>
            <ul>
              <li>
                <div class='highlight'>
                  <Code
                    wrap
                    code='<z-toggle-theme></z-toggle-theme>'
                    lang='html'
                  />

                  <div class='wc'>
                    <z-toggle-theme></z-toggle-theme>
                  </div>
                </div>
              </li>
            </ul>
          </details>
        </section>
      </article>

      <article class='info-content'>
        <h3>Custom Events:</h3>
        <section class='prop'>
          <details open>
            <summary>z-theme:changed</summary>
            <p>
              The <code>z-theme:changed</code> custom event is triggered whenever
              the click the component. It passes an event detail with the new theme
              value through the <code>dataTheme</code> property:
              <ul>
                <li>
                  payload: <code
                    >detail : &#123;dataTheme : "light" || "dark"&#125;
                  </code>
                </li>
              </ul>
            </p>
          </details>
        </section>
      </article>
    </div>
  </ContentLayout>

  <style>
    .info {
      display: flex;
      justify-content: center;
      height: 100%;
      gap: 5%;
    }
    .info-content {
      width: 50%;
      height: 100%;
      background-color: var(--aside-bg);
      border-radius: 5px;
      padding-left: 20px;
      padding-right: 20px;
      padding-bottom: 20px;
    }

    @media (max-width: 768px) {
      .info-content {
        padding: 0;
        padding-left: 7px;
      }
    }

    h2 {
      text-align: center;
    }

    .highlight {
      margin-top: 20px;
      max-width: max-content;
      gap: 20px;
      display: flex;
      flex-direction: column;
    }
    .wc {
      display: flex;
      flex-wrap: wrap;
      gap: 5px;
    }

    summary {
      font-weight: bold;
      font-size: medium;
      text-decoration: underline;
    }

    p {
      text-wrap: balance;
      font-size: 16px;
      line-height: 1.6;
      margin-bottom: 1em;
      word-spacing: 0.05em;
    }

    li {
      text-wrap: balance;
      list-style: none;
    }

    section.prop {
      margin-top: 50px;
    }

    code {
      padding: 5px;
      margin: 5px;
      background-color: var(--code-bg-color);
      border-radius: 5px;
    }
    @media (max-width: 768px) {
      p {
        font-size: 18px;
        line-height: 1.8;
      }

      .info {
        flex-direction: column;
      }

      hr {
        display: none;
      }
      .info-content {
        margin-bottom: 15%;
        width: 100%;
      }
    }
  </style>
</MainLayout>
